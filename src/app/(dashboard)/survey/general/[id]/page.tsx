import { SurveyDetailView } from '@/modules/survey';

export const metadata = {
    title: 'Detail Survey | dots CA',
    description: 'Detail survey data umum pendaftaran kredit',
};

export default function SurveyDetailPage() {
    return (
        <SurveyDetailView />
    );
}
