import { SurveyView } from '@/modules/survey';

export const metadata = {
    title: 'Survey Data Umum | dots CA',
    description: 'Manajemen survey data umum pendaftaran kredit',
};

export default function SurveyGeneralPage() {
    return (
        <SurveyView />
    );
}
